<!-- Converted from the complete Jira ticket source. -->

- [ ] Starting state: the tester has the required V4 launcher URL and any permitted access needed to open it. Action: navigate to that URL. Expected result: the V4 launcher page loads and is visible. Evidence: capture a non-empty PNG showing the loaded launcher page.
- [ ] Starting state: the V4 launcher page is loaded. Action: inspect each visible module card. Expected result: every visible module card exposes a clearly identifiable `Open` control. Evidence: capture a non-empty PNG in which the visible module cards and their `Open` controls can be identified.
- [ ] Starting state: the launcher page has loaded and the module-card controls are visible. Action: save the PNG evidence produced for this check. Expected result: the evidence is saved as a non-empty PNG file that can be opened and shows the visible controls. Evidence: verify the saved file exists, is non-empty, and renders successfully.
