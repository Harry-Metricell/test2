<!-- Converted from the complete Jira ticket source; each item has a testable starting state, action, and observable result. -->

# Acceptance Criteria

- [ ] Starting at the V4 launcher URL (`https://o2intelligence-v4-dev.metricell.com/launcher`) after signing in if prompted, verify that the launcher shows an `API Request Audit` module card with an `Open` control.
- [ ] From the launcher with the `API Request Audit` card visible, select `Open` and verify that the API Request Audit module loads on the same V4 host and no application load error is displayed.
