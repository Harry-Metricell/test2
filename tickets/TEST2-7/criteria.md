<!-- Converted from the complete Jira ticket source; each item has a testable starting state, action, and observable result. -->

<!-- Generated from Jira ticket TEST2-7. -->

- [ ] Starting from the V4 launcher, open GIS. The GIS workspace loads and the map, layer control, zoom controls, reset-map-view control, and map-style control are visible and usable.
- [ ] With the GIS workspace loaded, open the map layer control and enable one available coverage or network layer. The selected layer becomes visible on the map.
- [ ] With the GIS map displayed, activate the zoom-in control once. The map scale increases and the map remains centred on the current area.
- [ ] After changing the map view, activate the reset map view control. The map returns to its default view, and the GIS controls remain visible and usable.
- [ ] With the GIS map displayed, open the map style control and select a different available style. The map appearance changes, and the style control remains available for another selection.
