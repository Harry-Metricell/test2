<!-- Generated from Jira acceptance criteria. -->

- [ ] Given GIS is opened from the V4 launcher, then the GIS workspace loads with the map and controls visible.
- [ ] Given an available coverage or network layer is enabled, then the selected layer becomes visible on the map.
- [ ] Given the zoom-in control is used, then the map visibly changes scale while remaining centred on the current area.
- [ ] Given the reset map view control is used, then the map returns to its default view and GIS controls remain usable.
- [ ] Given another available map style is selected, then the map appearance changes and the style control remains available.
