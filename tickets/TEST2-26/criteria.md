<!-- Converted from the complete Jira ticket source; each item has a testable starting state, action, and observable result. -->

# Acceptance Criteria

- [ ] Given the configured V4 launcher page is available, open it and verify that at least one module card is visible with an Open module control.
- [ ] On a visible module card, select the Open module control and verify that the selected module page is displayed.
- [ ] After the module opens, verify that the resulting module page displays without a load error and capture evidence of both the launcher card and the opened module page.
